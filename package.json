{
  "name": "programmable-fade-v1",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-bound Programmable Uniswap v4 launch model with a 24-hour fee decay",
  "license": "MIT",
  "scripts": {
    "build": "forge build --offline",
    "fmt:check": "forge fmt --check",
    "manifest:verify": "node scripts/verify-model-manifest.mjs",
    "test": "forge test --offline",
    "test:ci": "FOUNDRY_PROFILE=ci forge test --offline"
  },
  "engines": {
    "node": ">=24"
  }
}
